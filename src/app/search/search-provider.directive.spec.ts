import { SearchProviderDirective } from './search-provider.directive';

describe('SearchProviderDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchProviderDirective();
    expect(directive).toBeTruthy();
  });
});
